# War Room OS V9.7 — Start Here

1. Run `CHECK_V97.bat`.
2. Copy `V97_TRADING.env.example` to `.env` and add a private approval secret.
3. Run `RUN_V97_PUBLIC_DATA.bat`.
4. Run `RUN_V97_QUOTES.bat`.
5. Run `RUN_V97_READINESS.bat`.
6. Run `RUN_V97_APP.bat`.
7. Use the process in `V97_OPERATOR_GUIDE.md` only after a real exact-scope proof is installed.

**Important:** the control plane is 5/5 operational. Approved alpha and fully prospective proof are still 0/5, so the default output is `NO_TRADE` and capital remains blocked.
