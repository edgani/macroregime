# Superseded v5.2 release controls

These files prove the deterministic v5.2 release that preceded this package. They are retained for
provenance only. Do not use them to validate v5.3. The active controls are
`PACKAGE_MANIFEST_V53.json`, `verify_manifest_v53.py`, `validate_user_v53.py`, and
`build_release_v53.py`.
