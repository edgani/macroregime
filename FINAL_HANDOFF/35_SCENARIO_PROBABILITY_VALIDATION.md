# Scenario Probability Validation

**Status: PROSPECTIVE_PENDING / UNKNOWN, intentionally fail-closed.**

The four attachment-driven scenarios are causal candidates, not calibrated probability forecasts. No numeric probability is emitted because this self-run environment does not contain a sufficiently large, preregistered historical event ontology with independent outcomes for these exact scenario classes.

Production rule: `probability = null`, `probability_status = UNKNOWN_UNCALIBRATED` until empirical base-rate and calibration tests exist. This prevents narrative plausibility from being converted into fake precision.
