# START HERE — V10.1

1. Run `SETUP_V101.bat` once.
2. Run `REFRESH_V101_NOW.bat` to collect current quotes, macro/rates, fundamentals, crypto context and release-lagged positioning.
3. Run `RUN_V101_APP.bat`.
4. Review **Macro & Risk → Carry Trade** and the selected **FX ticker packet**.
5. Run `RUN_V101_SHADOW.bat` only for eligible prospective records.
6. Run `CHECK_V101.bat` to validate the package.

Carry alpha is not marked proven until the historical point-in-time, untouched-lockbox, prospective-shadow and actual-fill gates pass. Current direction and beneficiaries remain usable research outputs while that proof accumulates.
