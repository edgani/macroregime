{
  "schema": "warroom.v66.prospective_shadow_protocol.v1",
  "purpose": "Create immutable point-in-time evidence for every future ticker, direction, target and risk-control claim before outcome maturity.",
  "capital_permission": "SHADOW_ONLY_ZERO_CAPITAL",
  "required_pre_outcome_fields": [
    "forecast_id", "created_at_utc", "component_id", "scope", "instrument", "orientation", "horizon_days",
    "feature_snapshot_sha256", "data_snapshot_sha256", "code_sha256", "previous_hash", "row_hash"
  ],
  "forbidden": [
    "editing a forecast after outcome information exists", "backfilling a forecast with a past timestamp", "capital allocation from an unmatured shadow record",
    "promoting aggregate archive evidence into ticker permission", "changing horizon or outcome definition after observation"
  ],
  "minimum_promotion_requirements": {
    "frozen_forecasts": 200,
    "distinct_market_regimes": 4,
    "calibration_not_worse_than_baseline": true,
    "simultaneous_out_of_sample_lower_bound_positive": true,
    "actual_cost_and_capacity_stress": true,
    "untouched_prospective_lockbox": true,
    "human_signed_exact_scope_receipt": true
  }
}
