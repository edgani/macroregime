> **Current release: V6.5 Proof-First Active Kernel.** Only components that pass their exact scope contract are active; predictive capital remains blocked until PIT, capacity, cost and prospective gates pass. See `V65_FINAL_STATUS.md`.

# v5.3 attachment continuation

This package now includes the reconciled v5.1 research ledger. Four narrow historical claims are visible as evidence only; V61 failed and V62 remains acquisition-blocked. Every such row has zero live decision weight and capital remains blocked. See `V53_ATTACHMENT_CONTINUATION_FINAL.md`.

# What is and is not proven in War Room OS v5.3

## Proven at the software-contract level

- The actual v4.2 source was inspected and hardened, rather than wrapping a separate toolkit around an unavailable application.
- The Python source compiles under the audit environment.
- Active persistent pickle/joblib/dill deserialization paths are absent.
- Canonical snapshots have schema, atomic writes, freshness controls and integrity hashes; changed runtime payloads are rejected.
- Editable proof-registry booleans cannot promote predictive output.
- Signed receipts are checked for exact component/scope/claim, signature, trust-root pin, expiry, revocation, artifact roles and hashes, required proof gates, matured prospective window and human approval.
- Direction authorization checks data source, dataset, as-of time, freshness, lineage, market rule and entry/stop/target geometry before consulting the exact-scope receipt.
- IHSG short authorization is blocked.
- Options capability is product- and row-specific; stale, crossed, incomplete or cross-market evidence fails closed.
- A boolean such as `probability_calibrated=true` cannot unlock expected value.
- Runtime numerical warnings are elevated to errors in the strict suites.
- Validators check return codes/timeouts and cannot modify immutable source unnoticed.
- Negative and planted-positive statistical controls behave correctly in the shipped validator.
- Synthetic and offline paths render a complete research snapshot while keeping promotion at zero and capital blocked.

## Structurally verified, but not semantically recomputed in this environment

The bundled Parquet files have valid `PAR1` framing, sane footer lengths, nonzero sizes and recorded SHA-256 hashes. Full semantic recomputation of every legacy Parquet research battery requires `pyarrow`; this environment does not provide it. The user-machine verifier installs it from `requirements.txt` and then runs the strict child-return-code wrapper.

## Environment-blocked here

The current build environment lacks `streamlit`, `yfinance`, `hmmlearn` and `pyarrow`. Therefore the actual Streamlit health endpoint and full dependency-complete research recomputation are not claimed as PASS here. They are mandatory user-machine gates.

## Not proven

- Current or universal long/short alpha.
- Physical target accuracy, exact timing, or cross-market generalization.
- Net profitability after live costs, market impact and capacity.
- Dealer ownership/sign from public option OI or prints.
- Institutional intent from public flow data.
- A production-winning HMM, Wasserstein model, order-flow model, bottleneck score or article-derived formula.
- Any V62 outcome. Its claim state and artifact hashes are reconciled, but the exact frozen protocol body, original runner, required archived price sources, and exact global-claim ZIP binary are not mounted. Reconstructing them would be a new study, not completion of V62.

## Current authorization

```text
predictive components promoted: 0
capital permission: BLOCKED
full predictive release: BLOCKED
```

Continuing to tune a used lockbox until it passes would be data-mining, not proof.

---

## v5.5 options/gamma continuation

**Proven at engineering/contract level:** exact-contract validation, public-OI unsigned semantics, verified-inventory sign gate, liquidity normalization, path-dependent delta-hedging accounting, V71 signed prospective ledger, adversarial tamper rejection, and zero-permission defaults.

**Not proven as trading edge:** dealer-gamma directional response, gamma-wall pin/break probability, exact move timing, net gamma-scalping profitability, or cross-market options generalization. Public OI and provider model labels never grant dealer sign or capital permission.
---

## v5.7 cusp structural-fragility continuation

**Implemented and tested:** stochastic cusp stationary-density estimation, fold/bistability geometry, macro-control mapping, fixed-universe breadth mapping, linear/smooth challengers, frozen time splits, 4,000-replication block bootstrap, simultaneous lower confidence bounds, shuffle/shift placebos, and one-time lockbox receipts.

**Result:** V73 modern macro, V74 breadth/internal transition, and V75 disjoint pre-1973 history all failed incremental prediction gates. Cusp sometimes improved descriptive return-density fit, but it worsened forward crash probability versus the frozen baseline. It is rejected from Crash Meter and live regime-transition logic.

```text
cusp predictive promotions: 0
cusp live decision weight: 0
cusp capital permission: BLOCKED
```

## V5.8 exhaustive research

**Proven engineering/accounting:** the versioned universe, 795-claim global trial ledger, source hashes, zero-live enforcement, and survivor queue are internally consistent and fail-closed.

**Not proven:** none of the three gross aggregate-return candidates is executable or cost-robust. The 114 placebo and 5 dropped definitions are mapped but their exact portfolio returns were not acquired. The remaining 656 candidates require source acquisition or exact reconstruction.

