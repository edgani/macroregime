# Scenario Timing Validation

**Status: PROSPECTIVE_PENDING.**

Timing is left `UNKNOWN_UNCALIBRATED` for current scenarios. Event calendars (FOMC, IPO filing/pricing, index eligibility/rebalance, refinancing/maturity schedules) are valid discriminating data, but no historical timing distribution has been built in the current runtime. The engine therefore lists the *next discriminating observations* rather than inventing a date.
