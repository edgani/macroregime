# Superseded v4.2 audit artifacts

These files are preserved only for provenance. Their manifests and verifier apply to the pre-hardening v4.2 source and must not be used to validate the v5.3 continuation package. The authoritative release controls are `PACKAGE_MANIFEST_V53.json`, `verify_manifest_v53.py`, `validate_user_v53.py`, and the `V53_*` reports.
